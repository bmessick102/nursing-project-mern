# Production build settings for Create React App.
# Do not emit readable source maps to end users in production.
GENERATE_SOURCEMAP=false
# Externalize CRA's inline runtime chunk so a future strict `script-src 'self'`
# CSP works without an inline-script hash/nonce.
INLINE_RUNTIME_CHUNK=false
